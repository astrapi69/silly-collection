# silly-collections .travis.yml

language: java

jdk:
    - openjdk8

addons:
    apt:
        update: true

notifications:
    email:
        recipients:
            - asterios.raptis@web.de
        on_failure: always 
               
after_success:
  - mvn -Pcoveralls clean test cobertura:cobertura jacoco:report coveralls:report